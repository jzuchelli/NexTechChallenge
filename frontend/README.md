# Frontend

This directory will house the Angular frontend for the HackerNewsAggregator project.

- `ng` or `nx` configuration files and source code should live here.
- Keep backend code isolated under `../backend`.
